<%= @user.email %>

<% @user.orders.each do |order| %>
  <% order.items.each do |item| %>
    <p> <%= item.product.title %></p>
    <p> <%= item.quantity %></p>
    <p> <%= item.product.price %></p>
    <% end %>
<% end %>
